# DISABLED - Workflow requires VERCEL_ORG_ID and VERCEL_PROJECT_ID secrets
# Re-enable by changing the trigger branch back to 'main'
name: Vercel Production Deployment (DISABLED)

concurrency:
  group: production-deploy
  cancel-in-progress: true

permissions:
  contents: read
  deployments: write
  id-token: write

on:
  push:
    branches:
      - main-disabled-temp

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'disabled'
